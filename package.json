{
  "name": "context-os-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root management for ContextOS MCP and CLI",
  "scripts": {
    "install:mcp": "cd workspace-mcp && npm install",
    "install:cli": "cd workspace-cli && npm install",
    "install:all": "npm run install:mcp && npm run install:cli",
    "build:mcp": "cd workspace-mcp && npm run build",
    "build:cli": "cd workspace-cli && npm run build",
    "build:all": "npm run build:mcp && npm run build:cli",
    "validate:all": "cd workspace-cli && npm start -- validate",
    "test:cli": "cd workspace-cli && npm start -- --help",
    "test:mcp": "cd workspace-mcp && npm test",
    "test:all": "npm run test:cli && npm run test:mcp",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
