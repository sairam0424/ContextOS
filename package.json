{
  "name": "context-os",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Enterprise-grade intelligence layer for autonomous AI agents.",
  "workspaces": [
    "workspace-cli",
    "workspace-mcp",
    "packages/core"
  ],
  "scripts": {
    "sync:assets": "node scripts/sync-templates.js",
    "build:cli": "npm run build -w @context-os/cli",
    "build:mcp": "npm run build -w @context-os/mcp",
    "build": "npm run sync:assets && npm run build:cli && npm run build:mcp",
    "test:cli": "npm test -w @context-os/cli",
    "test:mcp": "npm test -w @context-os/mcp",
    "test:all": "npm test",
    "test": "npm run test:cli && npm run test:mcp",
    "validate:all": "npm start -w @context-os/cli -- validate",
    "link:all": "npm run build && npm link -w @context-os/cli && npm link -w @context-os/mcp",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.13.10"
  }
}
