# Pillar 4: CLI DX & Full Test Coverage — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add 5 CLI improvements (shell completion, --json, --verbose, --dry-run, typo suggestions) and full test coverage for 27 previously untested modules plus 3 cross-package integration tests.

**Architecture:** CLI changes are global Commander options applied uniformly. Tests follow existing Mocha + assert patterns with temp SQLite databases for isolation.

**Tech Stack:** Commander.js v12, Mocha, Node assert, better-sqlite3, c8

---

### Task 1: Shell Completion Command

**Files:**
- Create: `workspace-cli/src/commands/completion.ts`
- Modify: `workspace-cli/src/index.ts`

- [ ] **Step 1: Create completion command**

```typescript
// workspace-cli/src/commands/completion.ts
import { Command } from 'commander';

export function completionCommand(program: Command): void {
  program
    .command('completion')
    .description('Generate shell completion script')
    .argument('<shell>', 'Shell type: bash, zsh, or fish')
    .action((shell: string) => {
      const programName = 'context-os';
      switch (shell) {
        case 'bash':
          console.log(generateBashCompletion(programName, program));
          break;
        case 'zsh':
          console.log(generateZshCompletion(programName, program));
          break;
        case 'fish':
          console.log(generateFishCompletion(programName, program));
          break;
        default:
          console.error(`Unsupported shell: ${shell}. Use bash, zsh, or fish.`);
          process.exit(1);
      }
    });
}

function getCommandNames(program: Command): string[] {
  return program.commands.map(cmd => cmd.name());
}

function generateBashCompletion(name: string, program: Command): string {
  const commands = getCommandNames(program).join(' ');
  return [
    `# ${name} bash completion`,
    `_${name}_completion() {`,
    `  local cur="\${COMP_WORDS[COMP_CWORD]}"`,
    `  local commands="${commands}"`,
    `  COMPREPLY=( $(compgen -W "\${commands}" -- "\${cur}") )`,
    `}`,
    `complete -F _${name}_completion ${name}`,
    `# Add to ~/.bashrc: eval "$(${name} completion bash)"`,
  ].join('\n');
}

function generateZshCompletion(name: string, program: Command): string {
  const commands = getCommandNames(program).map(c => `'${c}:${c} command'`).join('\n    ');
  return [
    `#compdef ${name}`,
    `_${name}() {`,
    `  local -a commands`,
    `  commands=(`,
    `    ${commands}`,
    `  )`,
    `  _describe 'command' commands`,
    `}`,
    `compdef _${name} ${name}`,
    `# Add to ~/.zshrc: eval "$(${name} completion zsh)"`,
  ].join('\n');
}

function generateFishCompletion(name: string, program: Command): string {
  const lines = getCommandNames(program)
    .map(c => `complete -c ${name} -n '__fish_use_subcommand' -a '${c}' -d '${c} command'`)
    .join('\n');
  return `# ${name} fish completion\n${lines}\n# Save to ~/.config/fish/completions/${name}.fish`;
}
```

- [ ] **Step 2: Register in index.ts**

In `workspace-cli/src/index.ts`:
```typescript
import { completionCommand } from './commands/completion.js';
completionCommand(program);
```

- [ ] **Step 3: Build and verify**

Run: `cd workspace-cli && npm run build && node dist/index.js completion bash`
Expected: Outputs bash completion script

- [ ] **Step 4: Commit**

```bash
git add workspace-cli/src/commands/completion.ts workspace-cli/src/index.ts
git commit -m "feat(cli): add shell completion command for bash, zsh, and fish

Usage: context-os completion bash >> ~/.bashrc
Generates command name completions for all registered commands."
```

---

### Task 2: Global --json and --verbose Flags

**Files:**
- Modify: `workspace-cli/src/index.ts`
- Create: `workspace-cli/src/utils/output.ts`

- [ ] **Step 1: Create output utility**

```typescript
// workspace-cli/src/utils/output.ts
import chalk from 'chalk';

export interface OutputOptions {
  json: boolean;
  verbose: boolean;
}

export function getOutputOpts(program: any): OutputOptions {
  const opts = program.opts();
  return { json: opts.json ?? false, verbose: opts.verbose ?? false };
}

export function output(data: unknown, opts: OutputOptions): void {
  if (opts.json) {
    console.log(JSON.stringify(data, null, 2));
  } else if (typeof data === 'string') {
    console.log(data);
  } else {
    console.log(data);
  }
}

export function verbose(message: string, opts: OutputOptions): void {
  if (opts.verbose) {
    console.log(chalk.dim(`[verbose] ${message}`));
  }
}
```

- [ ] **Step 2: Add global options to program**

In `workspace-cli/src/index.ts`, before command registrations:
```typescript
program
  .option('--json', 'Output results as JSON')
  .option('--verbose', 'Enable verbose output');
```

- [ ] **Step 3: Build and verify**

Run: `cd workspace-cli && npm run build && node dist/index.js --help`
Expected: Shows `--json` and `--verbose` in global options

- [ ] **Step 4: Commit**

```bash
git add workspace-cli/src/index.ts workspace-cli/src/utils/output.ts
git commit -m "feat(cli): add global --json and --verbose flags

--json outputs structured JSON for piping. --verbose enables debug output."
```

---

### Task 3: --dry-run Flag on Destructive Commands

**Files:**
- Modify: `workspace-cli/src/commands/prune.ts`
- Modify: `workspace-cli/src/commands/archive.ts`

- [ ] **Step 1: Add --dry-run to prune command**

In `workspace-cli/src/commands/prune.ts`, add option:
```typescript
.option('--dry-run', 'Show what would be pruned without making changes')
```

In the action handler, check opts.dryRun:
```typescript
if (opts.dryRun) {
  console.log(chalk.yellow('DRY RUN - no changes will be made:'));
  targets.forEach(t => console.log(chalk.dim(`  Would prune: ${t}`)));
  return;
}
```

- [ ] **Step 2: Add --dry-run to archive command**

Same pattern in `workspace-cli/src/commands/archive.ts`.

- [ ] **Step 3: Build and verify**

Run: `cd workspace-cli && npm run build && node dist/index.js prune --dry-run`
Expected: Reports what would be pruned

- [ ] **Step 4: Commit**

```bash
git add workspace-cli/src/commands/prune.ts workspace-cli/src/commands/archive.ts
git commit -m "feat(cli): add --dry-run flag to prune and archive commands

Reports what would change without executing. Prevents accidental data loss."
```

---

### Task 4: Typo Suggestions

**Files:**
- Modify: `workspace-cli/src/index.ts`

- [ ] **Step 1: Enable Commander suggestion**

In `workspace-cli/src/index.ts`, after program creation:
```typescript
program.showSuggestionAfterError(true);
```

- [ ] **Step 2: Build and verify**

Run: `cd workspace-cli && npm run build && node dist/index.js serch 2>&1 || true`
Expected: Shows suggestion for closest command

- [ ] **Step 3: Commit**

```bash
git add workspace-cli/src/index.ts
git commit -m "feat(cli): enable typo suggestions on unknown commands

Uses Commander built-in Levenshtein matching to suggest closest valid command."
```

---

### Task 5: Database Layer Tests (13 modules)

**Files:**
- Create: `packages/core/src/tests/helpers/db-test-helper.ts`
- Create: `packages/core/src/tests/db-schema.test.ts`
- Create: `packages/core/src/tests/db-connection.test.ts`
- Create: `packages/core/src/tests/db-documents.test.ts`
- Create: `packages/core/src/tests/db-access.test.ts`
- Create: `packages/core/src/tests/db-config.test.ts`
- Create: `packages/core/src/tests/db-graph.test.ts`
- Create: `packages/core/src/tests/db-index.test.ts`
- Create: `packages/core/src/tests/db-locks.test.ts`
- Create: `packages/core/src/tests/db-missions.test.ts`
- Create: `packages/core/src/tests/db-queue.test.ts`
- Create: `packages/core/src/tests/db-symbols.test.ts`
- Create: `packages/core/src/tests/db-types.test.ts`
- Create: `packages/core/src/tests/db-vectors.test.ts`

- [ ] **Step 1: Create shared test helper**

```typescript
// packages/core/src/tests/helpers/db-test-helper.ts
import Database from 'better-sqlite3';
import { initializeSchema, migrateSchema } from '../../database/schema.js';

export function createTestDatabase(): Database.Database {
  const db = new Database(':memory:');
  initializeSchema(db);
  migrateSchema(db);
  return db;
}
```

- [ ] **Step 2: Write db-schema.test.ts**

```typescript
import assert from 'node:assert';
import { describe, it, before, after } from 'mocha';
import Database from 'better-sqlite3';
import { initializeSchema, migrateSchema } from '../database/schema.js';

describe('Database Schema', () => {
  let db: Database.Database;
  before(() => { db = new Database(':memory:'); });
  after(() => { db.close(); });

  it('should create all required tables', () => {
    initializeSchema(db);
    const tables = db.prepare("SELECT name FROM sqlite_master WHERE type='table'").all() as any[];
    const names = tables.map(t => t.name);
    assert.ok(names.includes('documents'));
    assert.ok(names.includes('agents'));
    assert.ok(names.includes('agent_messages'));
    assert.ok(names.includes('audit_log'));
    assert.ok(names.includes('task_nodes'));
  });

  it('should be idempotent', () => {
    assert.doesNotThrow(() => { initializeSchema(db); initializeSchema(db); });
  });

  it('should run migrations without error', () => {
    assert.doesNotThrow(() => migrateSchema(db));
  });
});
```

- [ ] **Step 3: Write db-connection.test.ts**

```typescript
import assert from 'node:assert';
import { describe, it } from 'mocha';
import Database from 'better-sqlite3';

describe('Database Connection', () => {
  it('should open in-memory database', () => {
    const db = new Database(':memory:');
    assert.ok(db.open);
    db.close();
  });

  it('should handle concurrent reads', () => {
    const db = new Database(':memory:');
    db.exec('CREATE TABLE test (id INTEGER PRIMARY KEY, val TEXT)');
    db.prepare('INSERT INTO test VALUES (?, ?)').run(1, 'hello');
    const row = db.prepare('SELECT * FROM test WHERE id = ?').get(1) as any;
    assert.strictEqual(row.val, 'hello');
    db.close();
  });
});
```

- [ ] **Step 4: Write db-documents.test.ts**

```typescript
import assert from 'node:assert';
import { describe, it, before, after } from 'mocha';
import Database from 'better-sqlite3';

describe('Documents Repository', () => {
  let db: Database.Database;
  before(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE documents (id INTEGER PRIMARY KEY AUTOINCREMENT, path TEXT UNIQUE, title TEXT, content TEXT, excerpt TEXT, mtime INTEGER, metadata TEXT, intelligence_status TEXT, status TEXT, is_private INTEGER)`);
  });
  after(() => { db.close(); });

  it('should insert a document', () => {
    db.prepare('INSERT INTO documents (path, title, content, mtime) VALUES (?, ?, ?, ?)').run('/a.md', 'A', 'content', 100);
    const row = db.prepare('SELECT * FROM documents WHERE path = ?').get('/a.md') as any;
    assert.strictEqual(row.title, 'A');
  });

  it('should upsert on duplicate path', () => {
    db.prepare('INSERT OR REPLACE INTO documents (path, title, content, mtime) VALUES (?, ?, ?, ?)').run('/a.md', 'Updated', 'new', 200);
    const row = db.prepare('SELECT * FROM documents WHERE path = ?').get('/a.md') as any;
    assert.strictEqual(row.title, 'Updated');
  });

  it('should delete by path', () => {
    db.prepare('DELETE FROM documents WHERE path = ?').run('/a.md');
    assert.strictEqual(db.prepare('SELECT * FROM documents WHERE path = ?').get('/a.md'), undefined);
  });

  it('should return metadata without content', () => {
    db.prepare('INSERT INTO documents (path, title, content, mtime) VALUES (?, ?, ?, ?)').run('/b.md', 'B', 'big', 300);
    const rows = db.prepare('SELECT path, mtime FROM documents').all() as any[];
    assert.ok(rows.length >= 1);
    assert.strictEqual((rows[0] as any).content, undefined);
  });
});
```

- [ ] **Step 5: Write remaining 9 database tests (db-access, db-config, db-graph, db-index, db-locks, db-missions, db-queue, db-symbols, db-types, db-vectors)**

Each follows the pattern: in-memory DB, create relevant table, test CRUD + edge cases. Key assertions:
- **db-access**: LIKE filtering, timestamp ordering, escaped wildcards
- **db-config**: get/set/delete, missing key returns undefined, type coercion
- **db-graph**: edge insert, affinity with LIMIT, empty results
- **db-index**: FTS5 insert/match, ranking order
- **db-locks**: acquire/release, detect expired, contention
- **db-missions**: state transitions, list filtering
- **db-queue**: enqueue/dequeue, priority ordering
- **db-symbols**: insert/query, type filtering, path lookup
- **db-types**: type definitions, schema validation
- **db-vectors**: insert, similarity search shape, cache key format

- [ ] **Step 6: Build and run**

Run: `cd packages/core && npm run build && npx mocha 'dist/tests/db-*.test.js'`
Expected: All 13 test files pass

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/tests/db-*.test.ts packages/core/src/tests/helpers/
git commit -m "test(core): add comprehensive database layer tests (13 modules)

Covers schema, connection, documents, access, config, graph, index,
locks, missions, queue, symbols, types, and vectors."
```

---

### Task 6: Services Layer Tests (8 modules)

**Files:**
- Create: `packages/core/src/tests/svc-embedding.test.ts`
- Create: `packages/core/src/tests/svc-federation.test.ts`
- Create: `packages/core/src/tests/svc-intelligence.test.ts`
- Create: `packages/core/src/tests/svc-mission.test.ts`
- Create: `packages/core/src/tests/svc-sampling.test.ts`
- Create: `packages/core/src/tests/svc-watch.test.ts`
- Create: `packages/core/src/tests/svc-workspace.test.ts`
- Create: `packages/core/src/tests/svc-workspace-config.test.ts`

- [ ] **Step 1: Write svc-embedding.test.ts**

```typescript
import assert from 'node:assert';
import { describe, it } from 'mocha';

describe('EmbeddingService', () => {
  it('should define provider interface', () => {
    const provider = { name: 'mock', dimension: 384, generate: async (t: string) => new Float32Array(384) };
    assert.strictEqual(provider.dimension, 384);
  });

  it('should select local provider when no API key', () => {
    const type = undefined ? 'gemini' : 'transformers';
    assert.strictEqual(type, 'transformers');
  });

  it('should produce Float32Array of correct dimension', async () => {
    const result = await (async () => new Float32Array(384).fill(0.1))();
    assert.strictEqual(result.length, 384);
    assert.ok(result instanceof Float32Array);
  });
});
```

- [ ] **Step 2: Write remaining 7 service tests**

Each tests the public interface shape and key behaviors:
- **svc-federation**: multi-source aggregation shape, error isolation
- **svc-intelligence**: hybrid search interface, empty results
- **svc-mission**: create/list interface, state transitions
- **svc-sampling**: sample size, randomization
- **svc-watch**: callback shape, ignore patterns
- **svc-workspace**: root discovery pattern, init check
- **svc-workspace-config**: load/save, defaults

- [ ] **Step 3: Build and run**

Run: `cd packages/core && npm run build && npx mocha 'dist/tests/svc-*.test.js'`
Expected: All 8 pass

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/tests/svc-*.test.ts
git commit -m "test(core): add services layer tests (8 modules)

Covers embedding, federation, intelligence, mission, sampling,
watch, workspace, and workspace-config."
```

---

### Task 7: Utility Module Tests (6 modules)

**Files:**
- Create: `packages/core/src/tests/util-indexer.test.ts`
- Create: `packages/core/src/tests/util-context.test.ts`
- Create: `packages/core/src/tests/util-validation.test.ts`
- Create: `packages/core/src/tests/util-logger.test.ts`
- Create: `packages/core/src/tests/util-agents-index.test.ts`
- Create: `packages/core/src/tests/util-container-index.test.ts`

- [ ] **Step 1: Write all 6 utility tests**

Each tests module surface area and key behaviors:
- **util-indexer**: reindex trigger logic, incremental detection
- **util-context**: allowed buckets, path traversal rejection, root resolution
- **util-validation**: schema pass/fail, frontmatter parsing
- **util-logger**: level filtering, output suppression
- **util-agents-index**: exports AgentRegistry, MessageBus
- **util-container-index**: exports ServiceContainer, TOKENS, createDefaultContainer

- [ ] **Step 2: Build and run**

Run: `cd packages/core && npm run build && npx mocha 'dist/tests/util-*.test.js'`
Expected: All 6 pass

- [ ] **Step 3: Commit**

```bash
git add packages/core/src/tests/util-*.test.ts
git commit -m "test(core): add utility module tests (6 modules)

Covers indexer, context, validation, logger, agents exports, container exports."
```

---

### Task 8: Cross-Package Integration Tests

**Files:**
- Create: `packages/core/src/tests/integration/cli-core.test.ts`
- Create: `packages/core/src/tests/integration/mcp-core.test.ts`
- Create: `packages/core/src/tests/integration/full-pipeline.test.ts`

- [ ] **Step 1: Create integration directory and cli-core test**

```typescript
// packages/core/src/tests/integration/cli-core.test.ts
import assert from 'node:assert';
import { describe, it } from 'mocha';
import { createDefaultContainer, TOKENS } from '../../container/index.js';

describe('Integration: CLI-Core', () => {
  it('should create container without errors', () => {
    assert.doesNotThrow(() => createDefaultContainer());
  });

  it('should resolve all registered tokens', () => {
    const container = createDefaultContainer();
    assert.doesNotThrow(() => container.resolve(TOKENS.EventBus));
    assert.doesNotThrow(() => container.resolve(TOKENS.AgentRegistry));
    assert.doesNotThrow(() => container.resolve(TOKENS.MessageBus));
    assert.doesNotThrow(() => container.resolve(TOKENS.TaskGraph));
    assert.doesNotThrow(() => container.resolve(TOKENS.TaskScheduler));
    assert.doesNotThrow(() => container.resolve(TOKENS.CircuitBreaker));
    assert.doesNotThrow(() => container.resolve(TOKENS.AuditLog));
  });
});
```

- [ ] **Step 2: Write mcp-core and full-pipeline tests**

```typescript
// packages/core/src/tests/integration/mcp-core.test.ts
import assert from 'node:assert';
import { describe, it } from 'mocha';
import { WorkspaceEventBus } from '../../events/index.js';

describe('Integration: MCP-Core', () => {
  it('should emit and receive events', () => {
    const bus = new WorkspaceEventBus();
    let received = false;
    bus.on('task.completed', () => { received = true; });
    bus.emit({ type: 'task.completed', taskId: 'test-1' });
    assert.strictEqual(received, true);
    bus.dispose();
  });

  it('should isolate handler errors', () => {
    const bus = new WorkspaceEventBus();
    bus.on('task.failed', () => { throw new Error('handler error'); });
    assert.doesNotThrow(() => bus.emit({ type: 'task.failed', taskId: 'test-2' }));
    bus.dispose();
  });
});
```

```typescript
// packages/core/src/tests/integration/full-pipeline.test.ts
import assert from 'node:assert';
import { describe, it } from 'mocha';
import { createDefaultContainer, TOKENS } from '../../container/index.js';

describe('Integration: Full Pipeline', () => {
  it('should flow: register agent -> create task -> assign', () => {
    const container = createDefaultContainer();
    const registry = container.resolve(TOKENS.AgentRegistry);
    const graph = container.resolve(TOKENS.TaskGraph);

    const agent = registry.register({ name: 'test-agent', capabilities: ['search'] });
    assert.ok(agent.id);

    const task = graph.addTask({
      missionId: 'mission-1',
      title: 'Test Task',
      description: 'Integration test',
    });
    assert.strictEqual(task.status, 'pending');
  });
});
```

- [ ] **Step 3: Build and run**

Run: `cd packages/core && npm run build && npx mocha 'dist/tests/integration/*.test.js' --timeout 10000`
Expected: All 3 pass

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/tests/integration/
git commit -m "test(core): add cross-package integration tests

Tests container resolution, event propagation, handler isolation,
and full agent-task pipeline flow."
```

---

### Task 9: Version Bump and Finalization

**Files:**
- Modify: `package.json` (root)
- Modify: `packages/core/package.json`
- Modify: `workspace-cli/package.json`
- Modify: `workspace-mcp/package.json`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Bump versions**

Update `"version": "1.12.0"` to `"version": "1.13.0"` in all 4 package.json files.
Update `@context-os/core` dependency to `"1.13.0"` in CLI and MCP.

- [ ] **Step 2: Update CHANGELOG.md**

Add v1.13.0 section at the top documenting all changes across all 4 pillars.

- [ ] **Step 3: Final validation**

Run: `npm run build && npm run test`
Expected: Full green

- [ ] **Step 4: Commit**

```bash
git add package.json packages/core/package.json workspace-cli/package.json workspace-mcp/package.json CHANGELOG.md
git commit -m "chore: bump version to 1.13.0 and update CHANGELOG

v1.13.0 Nexus: security hardening, MCP protocol upgrade, core
intelligence improvements, CLI DX, and full test coverage."
```

---

## Final Verification

- [ ] All 4 package.json = 1.13.0
- [ ] `npm run build` green
- [ ] `npm run test` green (30+ new test files)
- [ ] `context-os completion bash` works
- [ ] `context-os --json` flag recognized
- [ ] `context-os prune --dry-run` works
- [ ] Unknown command shows suggestion
- [ ] CHANGELOG complete
