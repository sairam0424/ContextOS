name: Documentation

on:
  push:
    branches: [main]
    paths:
      - 'packages/core/src/**'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: 'npm'

      - run: npm ci

      - name: Build core package
        run: npm run build -w packages/core

      - name: Generate TypeDoc
        run: npx typedoc --entryPoints packages/core/src/index.ts --out docs-output --tsconfig packages/core/tsconfig.json

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs-output
          destination_dir: api
          keep_files: true
